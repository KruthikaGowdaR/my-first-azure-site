# Build your standalone Java file and check for compilation errors
trigger:
- main

pool:
  vmImage: ubuntu-latest

steps:
# 1. Compile your Java file directly using the Ubuntu built-in compiler
- script: |
    javac game.java
  displayName: 'Compile game.java'